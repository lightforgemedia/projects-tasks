title = "MCP Server E2E"
owner = "demo-team"

[[tasks]]
template = "backend_endpoint"
title = "MCP standalone E2E test"
role = "backend-dev"
[tasks.dod]
tests = ["go test ./projects/agenttools/..."]
manual = "Start MCP server (stdio) in-process and call echo tool via mcp-go client; repeat for HTTP if possible; record outputs in task comments"
