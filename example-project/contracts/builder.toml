[meta]
role = "builder"
version = "1.0.0"
issued_at = "2025-12-06T09:00:00Z"

[requirements]
must_have = ["goal.prompt", "scope.files", "success.criteria", "provenance.inputs"]

[fields.goal]
prompt = { type = "string", min_len = 10, description = "Instruction." }

[fields.scope]
files = { type = "array:string", must_exist = false, description = "Files to modify." }

[fields.success]
criteria = { type = "array:string", min_len = 1, description = "Test commands." }

[fields.provenance]
inputs = { type = "array:object", description = "Traceability." }
freshness_sec_max = 3600
